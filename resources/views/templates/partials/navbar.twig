<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="{{ path_for('home') }}">SlimRPG</a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li><a href="{{ path_for('home') }}">Strona główna</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                {% if auth.check %}
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{ auth.user.name }} <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path_for('auth.password.change') }}">Zmień hasło</a></li>
                        <li><a href="{{ path_for('auth.signout') }}">Wyloguj</a></li>
                    </ul>
                </li>
                {% else %}
                <li><a href="{{ path_for('auth.signup') }}">Zarejestruj się</a></li>
                <li><a href="{{ path_for('auth.signin') }}">Zaloguj się</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>